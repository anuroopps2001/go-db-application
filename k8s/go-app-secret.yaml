apiVersion: v1
kind: Secret
metadata:
  name: go-app-secret
  namespace: default
type: generic
stringData:
  DB_USERNAME: postgres
  DB_PASSWORD: mysecretpassword
  # Example:
  # password: {{ .Values.password | b64enc }}